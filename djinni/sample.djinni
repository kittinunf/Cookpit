sample_view_data = record {
  title: string;
}

sample_controller_observer = interface +o +j {
  on_update(view_data: sample_view_data);
}

sample_controller = interface +c {
  static create(): sample_controller;

  subscribe(observer: sample_controller_observer);
  unsubscribe();
}
